// let elements = document.getElementsByClassName('share-btn');
// Array.from(elements).forEach(element => {
//     element.addEventListener('click', copyText);
// });

// Alternatively (more modern approach):
